<Window x:Class="WpfTutorial.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTutorial"
        mc:Ignorable="d"
        Title="Names" Height="180" Width="260">

    <Window.Resources>
        <Style x:Key="MyStyle" TargetType="Label">
            <Setter Property="FontSize" Value="30px" />
        </Style>
    </Window.Resources>
    <Grid Margin="5,5,5,5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label>Names</Label>
        <ListBox Grid.Row="1" x:Name="listBoxNames">
        </ListBox>

        <StackPanel Grid.Column="1" Grid.Row="1" Margin="5,0,0,0">
            <TextBox x:Name="textBoxName" />
            <Button x:Name="buttonAdd" Margin="0,5,0,0" Click="ButtonAdd_Click">Add Name</Button>
        </StackPanel>
    </Grid>
</Window>
